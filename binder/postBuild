#!/bin/bash
set -ex

# 1. Clean up any pre-installed versions that might conflict
pip uninstall -y nglview ipywidgets widgetsnbextension notebook

# 2. Install the "Golden Trio" for Classic Notebook stability
# These three versions are known to work perfectly together in 2026
pip install "ipywidgets>=7.7.1,<8.0"
pip install "widgetsnbextension>=3.6.0,<4.0"
pip install "nglview==2.7.7"
pip install "notebook>=6.5.0,<7.0"

# 3. Install other tutorial dependencies
pip install matplotlib MDAnalysis mdtraj plotext ipympl jupyter_contrib_nbextensions

# 4. The "Model Found" Insurance Policy
# We manually copy the JS files to the system path and user path
jupyter nbextension install --py --sys-prefix widgetsnbextension
jupyter nbextension enable --py --sys-prefix widgetsnbextension

jupyter nbextension install --py --sys-prefix nglview
jupyter nbextension enable --py --sys-prefix nglview

# 5. Setup Contrib Extensions
jupyter contrib nbextension install --sys-prefix
jupyter nbextension enable scratchpad/main
jupyter nbextension enable rubberband/main
jupyter nbextension enable exercise2/main
jupyter nbextension enable toggle_all_line_numbers/main
jupyter nbextension enable execute_time/ExecuteTime
jupyter nbextension enable latex_envs/latex_envs

echo "Build complete."
